node_modules/
dist/
*.log
# Локальный адрес тест-VPS для --dev сборки (см. build.mjs). Не публиковать.
dev.local.json
# Адрес сервера + общий токен, который агент кладёт при установке. Токен не публиковать.
config.json
# Аудит-артефакты проверок (скриншоты/отчёты)
.audit/
